---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # - schematics.yaml
  - kubernetes.yaml
  # Disabled: current Talos plan reboots/retries without applying v1.13.2.
  # Re-enable after fixing tnu target/upgrade semantics.
  # - talos.yaml
